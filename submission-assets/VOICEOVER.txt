RECALL RESPONSE WORKBENCH — DEMO VOICEOVER
Planned finished runtime 2:38. Record as continuous, natural spoken prose.
Delivery: urgent and human at the open, calm and confident from the confirmation
gate onward. Let the two silent windows breathe; the picture is doing the work.

A supplier calls at nine in the morning: one lot of kettles can overheat.

Somewhere inside eight thousand orders are the few dozen that actually shipped
that lot — next to a nearly identical lot number that is perfectly
safe.

Miss one, and a hazard stays in somebody's kitchen. Over-recall, and you alarm
thousands of customers for nothing.

Today this is CSV exports and a spreadsheet war room, at the worst possible hour.

An assistant that only sees the screen doesn't help. It guesses which control to
click, and nobody can audit a guess.

So this page publishes its real operations instead. WebMCP turns the recall
itself — trace, classify, stage — into tools the browser hands to the agent.

Fresh Codex session, site tools on. Nine tools, registered once by the page, in
the order a recall actually happens.

One prompt, in the language an operations lead would actually use.

[pause — the first tool call lands]

Watch the page, not the transcript. Every call commits to the visible interface
before the agent gets its answer, so the human and the model share one source of
truth.

Exact-lot equality does the dangerous part. Thirty-seven recipients, and every
lookalike order deliberately held out — counted on screen as proof, not as a
promise.

Every match then gets exactly one obligation: hold it in the warehouse,
intercept it in transit, or call the person who already has it.

Quarantines, carrier intercepts, customer letters — all drafted, none released.
Durable actions, still zero.

So I tell it to just send everything.

[pause — the reply streams in]

There is nothing for it to call. No confirm tool, no commit tool, no send tool —
the consequential half of this workflow was never exposed as an API. That
guardrail is the shape of the interface, not a sentence in a prompt.

The last move stays human. I read the evidence, I take the decision, and the
receipt records that it came from the visible review control.

Now the agent reads back what was approved. The only tool left after the commit
is read-only.

The model does the precise, exhausting work. The page keeps every step
reviewable. And the irreversible click still belongs to a person. Give the agent
hands — keep the last decision human.
