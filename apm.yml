name: webmcp-recall-response-workbench
version: 1.0.0
description: APM project for webmcp-recall-response-workbench
author: Maxim Salnikov
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: agent-skills, antigravity, claude, codex, copilot, cursor, gemini, grok-build, kiro, opencode, windsurf
targets:
  - codex
  - copilot
dependencies:
  apm:
    - webmaxru/web-ai-agent-skills/skills/webmcp
  mcp: []
includes: auto
scripts: {}
